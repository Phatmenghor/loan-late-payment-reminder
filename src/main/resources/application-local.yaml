# Local Development Configuration (overrides base application.yaml)

server:
  port: ${SERVER_PORT:9393}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:loan_reminder}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123cp!@#}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: create

# Authentication
auth:
  username: ${AUTH_USERNAME:admin}
  password: ${AUTH_PASSWORD:admin123}

# CPB API Configuration
cpb:
  api:
    url: ${API_URL:http://localhost:9393}
    encryption-key: ${ENCRYPTION_KEY:OTTCPB!!BBDUNG#LOGFH$837990}

# Oracle Database Configuration
datasource:
  oracle:
    enabled: true
    url: ${ORACLE_URL:jdbc:oracle:thin:@192.168.102.5:1521:dwh}
    username: ${ORACLE_USERNAME:dwh}
    password: ${ORACLE_PASSWORD:Bnk$$444}
    driver-class-name: oracle.jdbc.driver.OracleDriver
    maximum-pool-size: 10
    minimum-idle: 3
    connection-timeout: 20000
    idle-timeout: 300000

logging:
  level:
    root: INFO
    com.backend: INFO
    org.springframework: WARN
    org.hibernate: WARN
