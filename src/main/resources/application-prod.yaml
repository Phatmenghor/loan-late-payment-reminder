# Production Configuration (overrides base application.yaml)

server:
  port: ${SERVER_PORT:9393}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:192.168.103.106}:${DB_PORT:5432}/${DB_NAME:loan_reminder}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123cp!@#}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: validate

# Authentication
auth:
  username: ${AUTH_USERNAME:admin}
  password: ${AUTH_PASSWORD:admin123}

# CPB API Configuration
cpb:
  api:
    url: ${API_URL:http://192.168.103.11:9393}
    encryption-key: ${ENCRYPTION_KEY:OTTCPB!!BBDUNG#LOGFH$837990}

# Oracle Database Configuration
datasource:
  oracle:
    url: ${ORACLE_URL:jdbc:oracle:thin:@192.168.102.5:1521:dwh}
    username: ${ORACLE_USERNAME:dwh}
    password: ${ORACLE_PASSWORD:Bnk$$444}
    driver-class-name: oracle.jdbc.driver.OracleDriver
    maximum-pool-size: 10
    minimum-idle: 3

logging:
  level:
    root: WARN
    com.backend: INFO
    org.springframework: WARN
    org.hibernate: WARN
