# Development Configuration
server:
  port: ${SERVER_PORT:5050}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:192.168.103.106}:${DB_PORT:5432}/${DB_NAME:internal_service}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:123cp!@#}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  flyway:
    enabled: false

# Authentication
app:
  auth:
    username: ${AUTH_USERNAME:admin}
    password: ${AUTH_PASSWORD:admin123}

# CPB API Configuration
cpb:
  api:
    url: ${API_URL:http://10.18.1.47:9393}
    encryption-key: ${ENCRYPTION_KEY:OTTCPB!!BBDUNG#LOGFH$837990}
    username: ${AUTH_USERNAME:admin}
    password: ${AUTH_PASSWORD:admin123}

# Oracle Database Configuration
datasource:
  oracle:
    enabled: true
    url: ${ORACLE_URL:jdbc:oracle:thin:@192.168.102.5:1521:stg}
    username: ${ORACLE_USERNAME:stg}
    password: ${ORACLE_PASSWORD:Bnk$$444}
    driver-class-name: oracle.jdbc.driver.OracleDriver
    maximum-pool-size: 10
    minimum-idle: 3
    connection-timeout: 20000
    idle-timeout: 300000
  oracle-dwh:
    enabled: true
    url: ${ORACLE_DWH_URL:jdbc:oracle:thin:@192.168.102.5:1521:dwh}
    username: ${ORACLE_DWH_USERNAME:dwh}
    password: ${ORACLE_DWH_PASSWORD:Bnk$$444}
    driver-class-name: oracle.jdbc.driver.OracleDriver
    maximum-pool-size: 10
    minimum-idle: 3
    connection-timeout: 20000
    idle-timeout: 300000

# Mobile Banking Configuration
mb:
  otpUrl: ${MB_OTP_URL:http://192.168.103.11:15004/CPBBank/services/CPBMobile?wsdl}
  registerCodeUrl: ${MB_REGISTER_CODE_URL:http://192.168.103.13:8090/API/RegisterCustomerMB/}
  secretKey: ${MB_SECRET_KEY:mobilebankingCPB@#%123}

logging:
  level:
    root: WARN
    com.backend: INFO
    org.springframework: WARN
    org.hibernate: WARN
