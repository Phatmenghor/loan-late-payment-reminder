# Development Server Configuration
server:
  port: ${SERVER_PORT:9393}

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:host.docker.internal}:${DB_PORT:5432}/${DB_NAME:loan_reminder}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Hour1819}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 15
      minimum-idle: 3
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  flyway:
    enabled: false

# Authentication
auth:
  username: ${AUTH_USERNAME:admin}
  password: ${AUTH_PASSWORD:admin123}

# CPB API Configuration
cpb:
  api:
    url: ${API_URL:http://10.18.1.47:9393}
    encryption-key: ${ENCRYPTION_KEY:OTTCPB!!BBDUNG#LOGFH$837990}

# Oracle Database Configuration
datasource:
  oracle:
    url: ${ORACLE_URL:jdbc:oracle:thin:@192.168.102.5:1521:dwh}
    username: ${ORACLE_USERNAME:dwh}
    password: ${ORACLE_PASSWORD:Bnk$$444}
    driver-class-name: oracle.jdbc.driver.OracleDriver
    maximum-pool-size: 5
    minimum-idle: 2
    connection-timeout: 20000
    idle-timeout: 300000

logging:
  level:
    root: INFO
    com.backend: INFO
    org.springframework: WARN
    org.hibernate: WARN
