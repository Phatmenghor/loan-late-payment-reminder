spring:
  application:
    name: cambodia_tiffany_furniture
  profiles:
    active: dev

  # Database Configuration
  datasource:
#    url: jdbc:postgresql://174.138.17.214:5432/tiffany_furniture
#    username: kithya
#    password: DE6Y6S30XO3KPTH

    url: jdbc:postgresql://192.168.103.106:5432/account_online_testing
    username: postgres
    password: 123cp!@#

    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          batch_size: 50

  # Cache Configuration
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=1h

# Server Configuration
server:
  port: 8080

# JWT Configuration
jwt:
  secret: da2bd42bef8f9f957d647ed13571df35b59c7055fb723eacdb05154878da17d77d3180385fc232cab38bded0fc7dbda0d439cc275e91164ade266d64d50633bc7
  # Access token expiration: 10 minutes (600000 milliseconds)
  expiration: 1111111111
  # Refresh token expiration: 1 month (2592000000 milliseconds = 30 days)
  refresh-token-expiration: 9999999999

# API Documentation
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true

# ===== TELEGRAM CONFIGURATION (MINIMAL) =====
telegram:
  bot:
    token: "8464259107:AAHdIC3EZb_3uID4vnhteY6PYtmO9WGDBjU"
    enabled: true
    group-chat-id: "-1002784141362"

# Application Configuration
app:
  name: "Tiffany Furniture Platform"
  version: "1.0.0"
  description: "Tiffany Furniture Platform for Cambodia"

  # Data Initialization
  init:
    create-admin: true
    admin-email: "phatmenghor19@gmail.com"
    admin-password: "88889999"

  # Security Configuration
  security:
    token-cleanup:
      enabled: true

# Logging Configuration
logging:
  level:
    root: INFO
    com.emenu: INFO
    org.springframework: WARN
    org.hibernate: WARN
    org.hibernate.SQL: WARN
    org.hibernate.type: WARN