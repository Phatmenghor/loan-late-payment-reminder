# ============================================================
# application.yaml  –  BASE CONFIG (shared across all profiles)
# ============================================================
# To switch environment:
#   profiles.active: local   →  localhost DB, debug logging
#   profiles.active: dev     →  dev server DB
#   profiles.active: prod    →  production DB, minimal logging
# ============================================================
spring:
  application:
    name: cambodia-platform
  profiles:
    active: prod # ← change to "local" or "dev" as needed

  # JPA  – common settings (ddl-auto overridden per profile)
  jpa:
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 50

  # Cache
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=1h

# Server
server:
  port: 8080

# JWT
jwt:
  secret: da2bd42bef8f9f957d647ed13571d35b59c7055fb723eacdb05154878da17d77d3180385fc232cab38bded0fc7dbda0d439cc275e91164ade266d64d50633bc7
  expiration: 8640000000 # 24 hours in milliseconds
  refresh-token-expiration: 604800000 # 7 days in milliseconds

# Swagger / OpenAPI
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true

# Application
app:
  name: "Cambodia Platform"
  version: "1.0.0"
  description: "E-Commerce Platform for Cambodia"
  init:
    create-admin: true
    admin-email: "phatmenghor19@gmail.com"
    admin-password: "88889999"
  security:
    token-cleanup:
      enabled: true

# Telegram
telegram:
  bot-token: "7964805821:AAGX_OucZ88VmJxhcDPC3fAPDHfaaP-nZOE"
  group-id: "-5114358941"
  enabled: true

# Authentication
auth:
  username: "admin"
  password: "admin123"

# CPB API Configuration
cpb:
  api:
    url: "http://localhost:8080"

# Notification Configuration
notification:
  sms:
    enabled: true
    api-endpoint: "/SendOTT"
    timeout-seconds: 30

# Oracle Database Configuration (Secondary - for reading data)
datasource:
  oracle:
    url: "jdbc:oracle:thin:@192.168.102.5:1521:dwh"
    username: "dwh"
    password: "Bnk$$444"
    driver-class-name: "oracle.jdbc.driver.OracleDriver"
    maximum-pool-size: 5
    minimum-idle: 2
    connection-timeout: 20000
    idle-timeout: 300000
